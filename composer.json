{
    "name": "my/app",
    "type": "project",
    "license": "GPL-3.0-or-later",
    "minimum-stability": "stable",
    "prefer-stable": true,
    "require-dev": {
        "squizlabs/php_codesniffer": "*",
        "sourcecroc/php-code-style": "^0.1.3"
    },
    "scripts": {
        "lint": "php -d memory_limit=-1 vendor/bin/phpcs -s --colors --standard=phpcs-ruleset.xml ."
    }
}
